# 秒杀系统技术架构文档（当前实现版）

## 1. 文档说明
- 文档目标：描述当前仓库已落地的真实技术架构，而非理想化生产集群方案。
- 代码基线：`d:\Gin秒杀API\seckill-go-master` + `d:\Gin秒杀API\seckill-vue-master`。
- 运行形态：单体后端 + 单页前端 + Redis + MySQL + RabbitMQ（本地/测试环境）。

## 2. 总体技术路线

### 2.1 架构风格
- 后端：Gin 单体服务，按 `controller/service/db/utils/model/router` 分层。
- 前端：Vue3 + Vite 单页应用，区分用户端与管理端页面。
- 数据层：MySQL 持久化、Redis 缓存与限流、RabbitMQ 异步消息。
- 安全层：JWT 认证、RBAC 鉴权、限流、防注入、行为分析、验证码校验。

### 2.2 当前部署形态
- 后端服务端口：`8081`
- 指标服务端口：`8082`（`/health`, `/metrics`）
- 前端开发端口：Vite 默认 `5176`（冲突时自动切换）
- 代理策略：前端通过 `/api` 反向代理到后端

## 3. 技术栈与选型

| 层级 | 技术 | 用途 |
|---|---|---|
| 前端 | Vue3, Vue Router, Axios, Vite | 用户端与管理端 UI、路由、接口调用 |
| 后端 | Go, Gin | HTTP API、业务逻辑、中间件编排 |
| 数据库 | MySQL + GORM | 用户、商品、订单、角色权限等持久化 |
| 缓存/风控 | Redis | 库存预热、限流、黑名单、风控状态 |
| MQ | RabbitMQ | 秒杀订单异步消息消费与重试 |
| 可观测性 | Zap + 自定义监控中间件 | 结构化日志、接口耗时、健康检查 |

## 4. 架构分层

### 4.1 前端层
- 用户端：商品浏览、秒杀下单、我的订单、个人中心。
- 管理端：用户管理、商品管理、订单管理、系统设置、日志、高并发测试、安全可视化。
- 路由控制：`/admin` 使用 `requiresAdmin` 守卫；`/stress-test`、`/security` 已重定向回用户首页，避免用户端入口暴露。

### 4.2 接口层（Controller）
- 统一处理请求参数校验、错误码返回、审计日志触发。
- 路由集中在 `router/router.go`，按业务模块分组。

### 4.3 业务层（Service）
- 用户、角色权限、库存、订单、活动、消息服务拆分。
- 秒杀核心路径：库存扣减 -> 订单创建（DB 优先）-> 异步消息处理。

### 4.4 基础设施层（DB/Redis/MQ）
- MySQL：订单、用户、角色权限、商品数据。
- Redis：限流器状态、黑名单、库存热数据与风控辅助。
- RabbitMQ：订单消息消费、死信与重试流程。

### 4.5 通用能力层（Utils）
- JWT 令牌生成与验证（issuer、有效期、算法校验）。
- 安全中间件（SQL 注入特征拦截、黑名单/限流检查）。
- 验证码服务与行为分析。
- 监控与审计日志。

## 5. 核心业务链路

### 5.1 登录与认证链路
1. 前端提交登录（用户名/手机号二选一 + 密码 + 验证码）。
2. 后端验证码校验通过后执行账号密码验证。
3. 生成 JWT（默认 24h），返回 `token` 与 `user_id`。
4. 受保护接口通过 `AuthMiddleware` 注入 `user_id` 上下文。

### 5.2 秒杀下单链路（当前实现）
1. 用户端调用 `POST /api/product/seckill`，必须携带 `Authorization`。
2. 中间件按顺序执行：认证 -> 行为分析 -> 验证码安全 -> 限流。
3. 服务层执行库存扣减（Redis 预热库存 + 扣减逻辑）。
4. 扣减成功后创建订单：
   - 优先写 MySQL（含 `order_no`）；
   - 不可用时降级到内存 mock 订单。
5. 管理端通过 `/api/order/admin/list` 可直接看到新订单，实现用户端与管理端同步。

### 5.3 订单管理链路
- 用户侧：`/api/order/list`, `/api/order/get/:id`, `/api/order/cancel/:id`。
- 管理侧：`/api/order/admin/list`, `/api/order/admin/get/:id`, `/api/order/admin/status/:id`。
- 状态枚举当前实现：
  - `0` 待支付
  - `1` 已支付
  - `2` 已取消

## 6. 安全架构

### 6.1 认证与授权
- JWT 鉴权：`HS256`，校验 issuer 与签名算法。
- RBAC：基于角色-权限模型，接口按权限码控制。
- 管理员兼容逻辑：若用户角色为“管理员”，可直接通过权限检查。

### 6.2 请求防护
- 全局安全中间件：
  - 可疑 SQL 注入特征检测（query/path/form）。
  - 安全检查失败返回统一错误结构。
- 验证码接口豁免：`/api/captcha` 不走全局限流拦截，避免验证码无法刷新。

### 6.3 限流与风控
- 多级限流：全局/IP/用户维度。
- 行为分析：结合 IP 与用户行为轨迹进行风险评估。
- 黑名单：支持按维度记录并检查。

### 6.4 数据与配置安全
- JWT 密钥支持环境变量注入：`JWT_SECRET`。
- 加密密钥支持环境变量派生：`ENCRYPTION_KEY`。
- 关键操作审计：登录、注册、秒杀等。

## 7. 可观测性与运维

### 7.1 监控
- `GET /health`：健康检查。
- `GET /metrics`：指标输出（供 Prometheus 抓取）。
- 接口监控中间件记录耗时、路径、状态码。

### 7.2 日志
- 全局使用 Zap 结构化日志。
- 包含业务日志、安全事件日志、审计日志。

### 7.3 启动流程
1. 初始化配置与日志。
2. 初始化 MySQL（并执行默认角色/权限/管理员初始化）。
3. 初始化 Redis（注入限流、安全模块；库存预热；启动对账任务）。
4. 初始化 RabbitMQ（订单消费者 + 消息重试服务）。
5. 初始化路由与指标服务，监听 `8081/8082`。

## 8. 当前前端架构要点
- 用户端导航已移除“高并发测试”“安全可视化”入口。
- 管理端内嵌高并发测试与安全可视化面板，统一后台风格。
- 管理端订单页已对齐后端状态定义并支持权限异常提示。

## 9. 已知约束与后续演进

### 9.1 当前约束
- 仍为单体后端部署，尚未拆分微服务。
- 部分测试接口（如重置库存/清理测试数据）仍对外保留。
- 生产级高可用（多副本、自动故障转移）未在本仓库直接实现。

### 9.2 建议演进
- 为测试接口增加管理员权限与环境开关。
- 完善订单领域模型（支付时间、支付流水、退款状态流转）。
- 将订单创建与库存确认进一步事务化/消息化，增强一致性。
- 增补自动化回归测试（鉴权、RBAC、订单同步、风控）。

## 10. 结论
当前项目已形成“可运行、可联调、具备基础安全治理能力”的秒杀系统技术架构：
- 在高并发路径引入 Redis + 限流 + 风控 + 验证码；
- 在管理能力上实现 RBAC 与订单统一视图；
- 在运维能力上具备日志、健康检查与基础指标输出。
该架构适合课程设计、实验环境和中小规模压力测试场景，并可持续向生产级能力演进。
