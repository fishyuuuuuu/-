﻿﻿﻿# 秒杀系统 PRD 与系统设计文档

## 1. 项目概述

### 1.1 项目背景
在高并发的电商秒杀场景中，系统往往面临流量洪峰、刷单攻击以及超卖风险。为保证核心业务在极端压力下的稳定性和公平性，本项目旨在设计一套包含高并发防护、多级限流、验证码安全和风控行为分析的秒杀系统。

### 1.2 项目目标
- 支撑高并发秒杀请求，确保库存扣减的原子性（不超卖）。
- 阻挡自动化脚本及刷单流量，保障普通用户体验。
- 提供用户端秒杀链路与管理端运营视图，并在统一的单体架构下实现高内聚开发。

### 1.3 核心功能一句话总结
一套具备动态验证码、智能限流、行为风控能力，基于 Redis 原子扣减库存并通过 MQ 异步处理订单的高并发秒杀系统。

---

## 2. 系统架构设计

### 2.1 整体架构图（文字版结构）
```text
[ 客户端层 ]
   ├── 用户端 (Vue3: 首页/商品/秒杀/订单)
   └── 管理端 (Vue3: 仪表盘/订单管理/高并发测试/安全可视化)
        │
[ 接入与控制层 (Gin HTTP) ]
   ├── 路由分组 (/api/user, /api/product, /api/order, /api/security)
   └── 安全中间件链 (认证 -> 行为风控 -> 验证码校验 -> 令牌桶限流)
        │
[ 核心业务层 (Go Service) ]
   ├── 权限服务 (RBAC 检查)
   ├── 商品/库存服务 (Redis Lua 预扣减)
   └── 订单服务 (MySQL 入库 + RabbitMQ 异步投递)
        │
[ 数据与基础设施层 ]
   ├── MySQL (关系型数据：用户、角色、商品、订单)
   ├── Redis (热点数据：库存缓存、限流令牌、黑名单、行为轨迹)
   └── RabbitMQ (异步解耦：订单队列、重试/死信机制)
```

### 2.2 分层架构
- **表现层 (Presentation)**：Vue3 + Vite 单页应用，分离前台商城与后台管理。
- **业务层 (Business)**：Gin Controller 负责参数校验，Service 负责原子扣减、消息发送及状态流转。
- **数据层 (Data)**：GORM 进行 MySQL 持久化操作；Go-Redis 客户端执行 Lua 脚本以保证操作的原子性。

### 2.3 技术栈说明
- **前端**：Vue 3, Vue Router, Axios, Vite。
- **后端**：Go 1.21+, Gin (Web 框架), GORM (ORM), Zap (结构化日志), golang-jwt (认证)。
- **基础设施**：MySQL 8.x, Redis 7.x, RabbitMQ 3.x。

### 2.4 外部依赖与服务集成
- **RabbitMQ**：订单削峰与重试。依赖 `seckill_order_queue` 及死信队列。
- **Redis**：依赖 Redis 作为限流器后端（基于 Lua）和库存锁控制中心。

---

## 3. 模块设计

### 3.1 核心模块职责
1. **用户与权限模块**：负责注册、登录（JWT），以及管理端 RBAC（基于角色/权限码）验证。
2. **秒杀控制模块**：处理高并发请求，执行验证码核验、限流拦截和行为风险评估。
3. **库存模块**：系统启动时从 MySQL 预热商品库存至 Redis；秒杀时通过 Lua 脚本在 Redis 中实现原子扣减。
4. **订单模块**：接收扣减成功的结果，优先将订单（包含生成的 `order_no`）持久化至 MySQL，并投递 MQ。
5. **安全可视化与压测模块**：在管理端实时展示限流拦截量、请求成功率，并提供模拟高并发发压测试功能。

### 3.2 模块间调用关系
- `HTTP Controller` -> 验证请求参数 -> 触发 `Security Middleware` -> 调用 `Product Service`。
- `Product Service` -> 请求 `Redis` 扣减库存 -> 若成功 -> 调用 `Order Service` 写入 DB 并投递至 `RabbitMQ`。

### 3.3 关键业务流程（文字步骤）
**秒杀下单流程**：
1. **鉴权与风控**：用户请求 `/api/product/seckill`，后端拦截器校验 JWT、验证码是否正确、行为是否异常（如 IP 高频访问）。
2. **限流检查**：通过 Redis 令牌桶算法，检查全局与 IP 级别请求频率是否超限。
3. **库存扣减**：进入服务层，通过 Redis Lua 脚本检查并原子的减去 1 个库存量。
4. **生成订单**：扣减成功后，在 MySQL 写入订单记录（状态：待支付），并发布消息至 RabbitMQ（供后续履约确认）。
5. **响应返回**：返回用户秒杀成功提示；用户可至“我的订单”或管理端查看最新订单。

---

## 4. 数据库设计

### 4.1 数据结构概览
采用关系型数据库存储核心实体，利用软删除 (`DeletedAt`) 保留数据轨迹。表之间通过 `id` 进行关联。

### 4.2 核心表结构
- **User (用户表)**
  - `id` (uint): 主键
  - `username` (varchar): 用户名 (唯一)
  - `password` (varchar): 哈希密码
  - `phone` (varchar): 手机号 (唯一)
- **Role & Permission (RBAC 权限表)**
  - `roles`, `permissions`, `user_roles`, `role_permissions`: 多对多关联表，存储角色和权限码（如 `order:read`）。
- **Product (商品表)**
  - `id` (uint64): 主键
  - `name` (varchar): 商品名称
  - `price` (decimal): 售价
  - `stock` (int): 原始库存
- **Order (订单表)**
  - `id` (uint): 主键
  - `order_no` (varchar): 业务单号 (唯一)
  - `user_id` (uint): 购买者 ID (关联 User)
  - `product_id` (uint): 商品 ID (关联 Product)
  - `status` (int): `0` 待支付, `1` 已支付, `2` 已取消

### 4.3 实体关系图 (ERD 文字版)
```text
[User] 1:N [Order]
[User] M:N [Role]
[Role] M:N [Permission]
[Product] 1:N [Order]
```

---

## 5. 接口设计

### 5.1 核心 API 列表
| 接口路径 | 方法 | 说明 | 鉴权与权限 |
| --- | --- | --- | --- |
| `/api/user/login` | POST | 用户/管理员登录 | 无 |
| `/api/captcha` | GET | 获取防刷图形验证码 | 无 (豁免全局限流) |
| `/api/product/seckill` | POST | 核心秒杀下单入口 | JWT 登录态 |
| `/api/order/list` | GET | 获取当前用户订单 | JWT 登录态 |
| `/api/order/admin/list` | GET | 获取全量订单 | JWT + `order:read` |
| `/api/order/admin/status/:id` | PUT | 更新订单状态 | JWT + `order:update` |

### 5.2 请求/响应格式示例
**秒杀请求 (POST /api/product/seckill)**
- 请求体：
  ```json
  { "productId": 1, "captchaId": "123x", "captchaStr": "7689" }
  ```
- 响应体：
  ```json
  { "message": "秒杀成功" }
  ```

### 5.3 权限与状态码规范
- `200 OK`: 成功。
- `400 Bad Request`: 参数错误、验证码错误或触发防注入策略。
- `401 Unauthorized`: 未登录或 Token 失效。
- `403 Forbidden`: 角色权限不足（如非管理员访问后台）。
- `429 Too Many Requests`: 触发 IP/用户/全局限流。

---

## 6. 业务逻辑设计

### 6.1 关键流程逻辑
- **库存预热**：系统 `main.go` 启动时，主动查询 `products` 表并将库存量加载到 Redis。
- **管理端初始化**：启动时若 `admin` 用户缺失，自动创建并赋予包含所有 `code` 的管理员角色。
- **状态流转**：订单创建初始状态为 `0`（待支付），后台通过列表的更新接口修改为 `1`（已支付）或 `2`（已取消）。

### 6.2 异常处理机制
- **数据库写入失败**：若库存 Redis 扣减成功但 MySQL 写入订单失败（如网络抖动），当前会记录 Error 日志并走补偿对账机制。
- **MQ 投递失败**：利用死信队列 (`dlx_queue`) 与定时重试协程兜底。

### 6.3 性能与安全要点
- **性能**：订单插入数据库动作可异步化（由 RabbitMQ 消费落库），降低接口同步 RT。
- **安全**：使用环境变量派生 AES 密钥（`ENCRYPTION_KEY`），统一上下文取 `user_id` 避免越权。验证码作为秒杀的强制前置条件。

---

## 7. 部署与运维说明

### 7.1 部署环境需求
- 操作系统：Linux/Windows/macOS。
- 中间件：MySQL 8+, Redis 7+, RabbitMQ 3.10+（需开放 `5672` 及管理端 `15672` 端口）。

### 7.2 启动流程
1. 配置 `config.yaml`（填写正确的 DB、Redis 和 MQ 凭证）。
2. 在前端目录执行 `npm install && npm run build`（或 `npm run dev`）。
3. 在后端目录执行 `go run .`（或构建二进制运行）。
4. 系统启动后自动侦听 `8081` 提供 API 服务，`8082` 提供监控指标。

### 7.3 监控指标
- `GET /health`：返回当前系统健康状态。
- `GET /metrics`：返回请求次数、耗时直方图、限流拦截次数（Prometheus 格式）。
- **后台安全可视化**：提供实时 QPS 和黑名单拦截拦截统计。

### 7.4 常见问题与排查
- **验证码刷新不出来**：检查 Redis 是否已满，或当前 IP 是否被触发 `429` 误伤拦截（确认安全中间件豁免逻辑已生效）。
- **下单 500 报错**：检查 RabbitMQ 队列 `seckill_order_queue` 是否声明成功，或者 MySQL 订单表 `order_no` 是否存在约束冲突。

---

## 8. 扩展计划

### 8.1 可迭代功能
- **支付履约模块**：对接微信/支付宝沙箱，实现从“待支付”到“已支付”的真实资金链路。
- **复杂检索**：管理端订单列表增加按手机号、时间范围、商品名称的模糊查询和服务器端分页。

### 8.2 性能优化方向
- **微服务化**：将用户、订单、库存拆分为独立的 gRPC 服务。
- **读写分离**：为 MySQL 引入主从集群，将后台的大批量查询压力与用户下单压力隔离。
- **本地缓存**：在 Redis 之前引入 FreeCache/BigCache 处理极端热点商品的元数据读取。
